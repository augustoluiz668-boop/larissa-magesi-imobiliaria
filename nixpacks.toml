[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3.12 -m pip install -r backend/requirements-prod.txt"]

[start]
cmd = "cd backend && python3.12 -m uvicorn server:app --host 0.0.0.0 --port $PORT"
