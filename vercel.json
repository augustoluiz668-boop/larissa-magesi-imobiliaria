{
  "buildCommand": "cd frontend && yarn install && CI=false GENERATE_SOURCEMAP=false yarn build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}

